para = (function (Kinetic, tek, para) {
    para.Page = tek.define({
        init: function (data) {
            var s = this;
        },
        properties: {

        }
    });
    return para;
})(Kinetic, tek, window['para'] || {});